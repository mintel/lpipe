language: python
python:
  - "3.6"
  - "3.7"
  - "3.8"
services:
  - docker
install:
  - make init
  - make build-test-lambda
jobs:
  include:
    - stage: test
      script: make pytest
    - stage: deploy
      script: make bh/debug
